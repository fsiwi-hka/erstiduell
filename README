This is a netbeans project. Use either Netbeans to compile it or Ant.
If you want to use Ant just type:

$ ant

to compile and:

$ ant run

to run. Once compiled there is also a JAR file in the dist folder that u can use
everwhere:

$ java -jar erstiduell.jar


Please read LICENSE file.

For questions visit our IRC at irc.freenode.net ##fsi

Usage should be self explaining:

- Start software
- Choose question library
- Choose buzzer buttons for each team
- Choose display for game window

- You can change the team name in the Gamecontroller window by double clicking the team name box

*BACKLOG (no order)
** fix error in JComboBox (probably racecondition, since it does not occur in debug mode)
** enable controlling game via keyboard
** make number of remaining tries for answering question visible
